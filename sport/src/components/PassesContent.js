import React, { Component } from 'react';

class PassesContent extends Component {
  render() {
    return(
      <div className="content">Passes Page</div>
    );
  }
}

export default PassesContent;
